## 一、最终技术栈

| 层级      | 技术                                         | 主要用途                             |
| --------- | -------------------------------------------- | ------------------------------------ |
| 后端语言  | **Java 21**                                  | 服务端业务开发                       |
| 后端框架  | **Spring Boot**                              | REST API、业务逻辑、配置管理         |
| Web 框架  | Spring MVC                                   | HTTP 接口                            |
| 权限安全  | **Spring Security + JWT**                    | 登录、认证、RBAC权限                 |
| ORM       | **MyBatis-Plus**                             | MySQL 数据访问                       |
| 数据库    | **MySQL 8.4 LTS**                            | 核心业务数据                         |
| 缓存      | **Redis**                                    | 缓存、验证码、热点数据、秒杀、幂等   |
| 搜索      | **Elasticsearch**                            | 商品全文搜索、筛选、搜索建议         |
| 日志检索  | **Elasticsearch + Filebeat + Kibana**        | 集中日志采集、查询、分析             |
| Java辅助  | **Lombok**                                   | 减少 Getter/Setter、构造器等模板代码 |
| API文档   | OpenAPI + Knife4j                            | 后端接口文档和调试                   |
| Web用户端 | **Vue 3 + Vite + TypeScript**                | PC/Web商城                           |
| Web管理端 | **Vue 3 + Vite + TypeScript + Element Plus** | 管理员、商家、客服后台               |
| App       | **Flutter + Dart**                           | Android App                          |
| 小程序    | **微信原生小程序 + TypeScript**              | 微信小程序                           |
| 实时通信  | **WebSocket**                                | 客服聊天、实时消息                   |
| 版本管理  | Git + GitHub                                 | 五人协同开发                         |
| 构建工具  | Maven                                        | Java依赖及构建                       |
| 部署      | Docker / Docker Compose（后期）              | MySQL、Redis、ES等环境统一           |

Java 21 仍属于 LTS 版本，而且与当前 Spring Boot 的 Java 要求兼容；当前 Spring Boot 4.1 至少要求 Java 17。对于课程项目我更倾向于 **Java 21**，不必追 Java 25/26。

MySQL 8.4 是官方 LTS 系列，也适合作为你们全组统一的数据库版本

## 二、后端不要做成微服务

虽然你们五个人已经划分：

```
身份与用户
商家
商品
交易
聊天
```

但目前仍然建议：

> **模块化单体 Spring Boot**

不要五个人分别启动五个 Spring Boot 服务。

也就是：

```
backend/
└── 一个 Spring Boot 应用
```

内部按照模块划分：

```
backend/
└── src/main/java/com/xxx/mall/
    │
    ├── common/
    │
    ├── security/
    │
    ├── auth/
    │
    ├── user/
    │
    ├── statistics/
    │
    ├── merchant/
    │
    ├── product/
    │
    ├── search/
    │
    ├── review/
    │
    ├── cart/
    │
    ├── trade/
    │
    ├── payment/
    │
    ├── chat/
    │
    ├── message/
    │
    └── contentsafety/
```

Spring Boot 官方定位本身就是快速构建可独立运行的 Spring 应用。

这种方式特别适合你们五个人：

```
成员1
auth/user/security/statistics

成员2
merchant

成员3
product/search/review

成员4
cart/trade/payment

成员5
chat/message
```

大家仍然在一个后端工程里开发。

## 三、后端公共技术

### 1. Spring Boot

负责：

```
项目启动
REST API
依赖管理
配置管理
异常处理
数据库连接
Redis
Elasticsearch
WebSocket
```

------

### 2. MyBatis-Plus

负责：

```
CRUD
分页
条件查询
数据库映射
```

例如：

```
public interface UserMapper extends BaseMapper<User> {
}
```

商品：

```
public interface ProductMapper extends BaseMapper<Product> {
}
```

MyBatis-Plus 官方目前同时提供 Spring Boot 3/4 的接入方式。

复杂查询仍然允许写 XML：

```
商品销量排行榜
订单统计
商家经营统计
复杂 JOIN
```

不要强制所有 SQL 都用 Wrapper。





## 四、Redis 后期加入，但是建议架构现在预留

你们项目非常适合 Redis。

例如：

### 登录模块

```
验证码
Token黑名单
用户登录状态
```

### 商品模块

```
热门商品缓存
商品详情缓存
搜索热词
```

### 交易模块

```
防重复提交
分布式锁
临时结算信息
```

### 秒杀

```
秒杀库存
用户限购
活动状态
```

### 聊天

```
在线用户
在线客服
未读数
```

第一阶段不用什么都缓存。

顺序应该是：

```
先实现 MySQL 版本
        ↓
系统跑通
        ↓
识别热点
        ↓
加入 Redis
```

## 五、Elasticsearch

而且可以承担两个非常适合你们项目的任务：

```
① 日志检索
② 商品搜索
```

Elasticsearch 官方明确支持全文搜索，同时 Elastic Stack 也支持日志、指标和 Trace 等可观测数据。

但这两个用途建议使用**不同索引**。

例如：

```
Elasticsearch

├── mall-product-v1
│   商品搜索索引
│
├── mall-log-backend-2026.08
│   后端日志
│
└── mall-operation-log-v1
    操作审计
```

不要把商品和日志混在一起。

## 六、Elasticsearch 做日志

推荐的数据流：

```
Spring Boot
     │
   Logback
     │
     ▼
logs/
├── application.log
├── error.log
└── audit.log
     │
     ▼
  Filebeat
     │
     ▼
Elasticsearch
     │
     ▼
   Kibana
```

Filebeat 官方就是用于监听日志文件并将事件发送至 Elasticsearch 或 Logstash。

这样你可以在 Kibana 搜：

```
level = ERROR
```

或者：

```
userId = 10001
```

或者：

```
requestId = abc123
```

甚至：

```
/order/create AND ERROR
```

## 七、日志最好结构化

不要全是：

```
用户登录成功
订单失败
程序异常
```

建议日志记录：

```
{
  "timestamp": "2026-08-08T15:30:21",
  "level": "INFO",
  "service": "mall-backend",
  "traceId": "a82bf917",
  "userId": 10001,
  "module": "order",
  "action": "CREATE_ORDER",
  "message": "order created",
  "orderId": 900001
}
```

以后 Kibana 查问题特别舒服：

```
traceId
→ 找到一次请求所有日志
```

你负责审计模块的话，这一点尤其值得做。

## 八、Elasticsearch 做商品搜索

商品负责人原来负责：

```
索引
检索
筛选
排序
建议
热词
```

那么 Elasticsearch 正合适。

例如商品：

```
Apple iPhone 17 Pro 256GB 黑色
```

用户搜索：

```
iphone
苹果手机
iphone pro
```

比纯：

```
LIKE '%iphone%'
```

更适合做相关性搜索。

Elasticsearch 的 `text` 字段会进行分析和建立全文索引，而全文查询也是其核心搜索能力。

## 九、但是 Elasticsearch 商品数据不是主数据

非常重要。

应该：

```
                  MySQL
                    │
            商品真实业务数据
                    │
                    ▼
             Elasticsearch
              商品搜索索引
```

例如：

```
MySQL product
id = 1001
name = iPhone
price = 5999
stock = 100

          ↓ 同步

Elasticsearch
product_id = 1001
name = iPhone
price = 5999
...
```

如果 ES 挂了：

```
用户暂时无法搜索
```

可以接受。

但是：

```
订单
库存
支付
```

不能因此丢失。

所以：

> **MySQL = Source of Truth**

> **Elasticsearch = Search Index**

## 十、商品搜索索引可以设计成

例如：

```
{
  "productId": 1001,
  "spuId": 5001,
  "shopId": 3001,

  "name": "Apple iPhone",
  "subtitle": "旗舰智能手机",

  "categoryId": 101,

  "brand": "Apple",

  "priceMin": 5999,
  "priceMax": 8999,

  "sales": 10000,
  "rating": 4.9,

  "tags": [
    "手机",
    "5G",
    "旗舰"
  ],

  "status": "ON_SALE"
}
```

然后能够实现：

```
关键词搜索
分类筛选
品牌筛选
价格区间
销量排序
价格排序
评分排序
```

## 十一、MySQL → Elasticsearch 如何同步？

这个问题你们一定会遇到。

第一版课程项目建议：

```
商品新增/修改
     ↓
先写 MySQL
     ↓
事务成功
     ↓
同步 Elasticsearch
```

比如：

```
ProductService
     │
     ├── MySQL update
     │
     └── SearchIndexService.update()
```

但如果：

```
MySQL成功
ES失败
```

怎么办？

增加：

```
重新同步接口
```

例如后台：

```
POST /admin/search/reindex/products
```

能够：

```
MySQL
 ↓
全量重建
 ↓
Elasticsearch
```

这样课程项目已经足够可靠。

以后有时间可以升级成：

```
MySQL
 ↓
业务事件
 ↓
MQ
 ↓
Elasticsearch
```

但现在**不要急着加 Kafka/RabbitMQ**



## 十二、Spring Security + JWT

你负责这一块。

结构建议：

```
用户登录
   ↓
用户名密码
   ↓
Spring Security
   ↓
Authentication
   ↓
生成 JWT
   ↓
前端保存 Token
```

之后：

```
GET /api/orders
Authorization: Bearer eyJ...
```

后端：

```
JWT Filter
    ↓
验证JWT
    ↓
解析userId
    ↓
加载权限
    ↓
SecurityContext
    ↓
Controller
```

Spring Security 原生支持使用 Bearer JWT 来保护接口。

## 十三、权限采用 RBAC

你们之前规划得很好：

```
USER
普通用户

MERCHANT_OWNER
店主

MERCHANT_STAFF
商家员工

CUSTOMER_SERVICE
客服

ADMIN
管理员

SUPER_ADMIN
超级管理员
```

数据库：

```
user

role

permission

user_role

role_permission
```

例如：

```
ADMIN
 ↓
product:audit
merchant:audit
user:disable
statistics:view
```

商家：

```
MERCHANT_OWNER
 ↓
product:create
product:update
order:ship
review:reply
```

------

## 十四、API 文档

建议：

```
OpenAPI 3
+
Knife4j
```

Knife4j 是基于 Swagger/OpenAPI 的增强文档方案，并支持 OpenAPI 3。

这样前端三个团队都不用问：

> 登录接口参数是什么？

直接打开：

```
/doc.html
```

查看。

每一个 Controller 都应完整描述：

```
请求方法
URL
参数
返回值
状态码
权限
```

------

## 十五、Web 用户端

建议：

```
Vue 3
TypeScript
Vite

Vue Router
Pinia
Axios
```

Vue 官方目前对新 Vue 项目推荐使用 Vite，并且官方 `create-vue` 可以直接创建支持 TypeScript 的 Vite 项目。

目录：

```
frontend-web/
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── layouts/
│   ├── router/
│   ├── stores/
│   ├── types/
│   ├── utils/
│   └── views/
```

------

## 十六、管理端

技术：

```
Vue 3
TypeScript
Vite
Element Plus
Vue Router
Pinia
Axios
```

不要再分别建立：

```
admin-web
merchant-web
customer-service-web
```

可以统一：

```
frontend-admin
```

登录后根据角色控制菜单：

```
ADMIN
│
├── 用户管理
├── 商家审核
├── 商品审核
└── 统计

MERCHANT_OWNER
│
├── 店铺
├── 商品
├── 订单
└── 统计

CUSTOMER_SERVICE
│
├── 会话
└── 消息
```

------

## 十七、Flutter App

Flutter 官方目前支持 Android 部署，使用 Android Studio 即可在 Android 模拟器或实体设备上开发调试。

建议：

```
Flutter
Dart
Dio
Riverpod 或 Provider
go_router
```

如果团队第一次学 Flutter：

> 状态管理选一个就行，不要 Riverpod、Bloc、GetX 全部上。

我比较倾向：

```
Dio
+
Riverpod
+
go_router
```

------

## 十八、微信小程序

采用：

```
微信原生小程序
+
TypeScript
```

结构：

```
frontend-miniapp/
├── pages/
├── components/
├── services/
├── utils/
├── types/
└── app.ts
```

Web、Flutter、小程序三端全部请求：

```
同一个 Spring Boot
```

------

## 十九、WebSocket

聊天负责人：

```
WebSocket
+
MySQL
+
Redis（后期）
```

职责分开：

```
WebSocket
→ 实时传递

MySQL
→ 保存聊天记录

Redis
→ 在线状态 / 未读状态 / 临时会话
```

比如：

```
用户发送消息
    ↓
WebSocket
    ↓
Spring Boot
    ├── 写入MySQL
    └── 推送客服
```

------

## 二十、最终项目目录建议

现在 Git 仓库最终建议改成：

```
ecommerce-platform/
│
├── backend/
│   └── Spring Boot
│
├── frontend-web/
│   └── Vue 3
│
├── frontend-app/
│   └── Flutter
│
├── frontend-miniapp/
│   └── 微信原生小程序
│
├── frontend-admin/
│   └── Vue 3 + Element Plus
│
├── deploy/
│   ├── mysql/
│   ├── redis/
│   ├── elasticsearch/
│   ├── kibana/
│   └── filebeat/
│
├── database/
│
├── docs/
│   ├── architecture/
│   ├── api/
│   ├── database/
│   ├── git/
│   └── requirements/
│
├── docker-compose.yml
├── README.md
└── .gitignore
```