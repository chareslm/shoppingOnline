# Copy to .env for local overrides (.env is gitignored).
# [OPTIONAL] Default already matches Compose BACKEND_PORT=8080.
# Change only when the backend is not http://localhost:8080
# Admin Vite port is 5173 (see vite.config.ts); do not put secrets here.
VITE_API_BASE_URL=http://localhost:8080
