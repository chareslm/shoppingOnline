# V8 审计日志查询权限

迁移文件：`database/V8__audit_log_view_permission.sql`。

本迁移新增 `system:audit:view` 权限，用于管理端分页查询平台安全审计日志，并默认仅授予内置 `SUPER_ADMIN` 角色。V1 已包含 `audit_log` 表及操作者、动作、目标、请求元数据和 JSON 详情字段，因此 V8 不修改表结构。

迁移使用幂等写法，可由 Flyway 在 V7 后自动执行。不得将该权限直接授予普通用户或没有平台安全治理职责的业务角色；如需开放给其他管理角色，必须先补充明确的数据范围规则和脱敏验收。
